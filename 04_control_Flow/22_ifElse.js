
// other things are same as in another language
const age = 20

if(age < 18) {
    console.log(`your age is less than 18 you can not vote`);
} else {
    console.log(`your age is greater than 18 you can vote`);
}